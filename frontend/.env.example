# Leave empty during local development — Vite's dev server proxies /api and
# /uploads straight to your backend (see vite.config.js), so no URL is needed.
#
# In production, set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://civicsense-api.onrender.com
VITE_API_URL=
